package pl.mealplanner.loginandregister.infrastructure;

import lombok.AllArgsConstructor;
import org.springframework.stereotype.Controller;

@AllArgsConstructor
@Controller
class TokenController {

    //JwtAuthenticatorFacade jwtAuthenticatorFacade;

//    @PostMapping("/token")
//    public JwtResponseDto authenticateAndGenerateToken(@Valid @RequestBody TokenRequest tokenRequest) {
//        final JwtResponseDto jwtResponse = jwtAuthenticatorFacade.authenticateAndGenerateToken(tokenRequest);
//        return jwtResponse;
//    }
}
